---
name: unicorn-ops
description: แนวทางการดำเนินงานและการพัฒนาโปรเจกต์ Unicorn Smart AI เป็นภาษาไทย
---

# 🦄 Unicorn Smart AI - แนวทางการดำเนินงาน (Ops Guide)

คู่มือนี้รวบรวมมาตรฐานและขั้นตอนการพัฒนาโปรเจกต์ Unicorn Smart AI เพื่อรักษาคุณภาพและทัศนคติแบบมืออาชีพ (UBC Coach)

## 1. มาตรฐานการพัฒนา (Development Standards)

### ประสิทธิภาพของ React (Performance)

- **Hoisting Static Data:** ย้ายอาเรย์ข้อมูลคงที่ (static list) ออกนอก Component เสมอ เพื่อลดภาระการประมวลผล (re-render)
- **Memoization:** ใช้ `useMemo` หรือ `useCallback` สำหรับการคำนวณที่ซับซ้อนหรือฟังก์ชันที่ส่งต่อให้ Component ลูก

### งานดีไซน์และความสวยงาม (UI/UX Aesthetics)

- **Typography:** ใช้ `text-xs-plus` (12px) แทนการใช้ `10px` เพื่อให้อ่านง่ายขึ้น
- **Glassmorphism:** ใช้ `glass-card` หรือ `glass-card-dark` พร้อมค่า Blur ที่เหมาะสม
- **Animations:** ใช้ `animate-fade-in` หรือ `animate-float-slow` เพื่อให้เว็บดูมีชีวิตชีวา
- **Emojis:** ใช้เพื่อเพิ่มพลังบวก (Positive Energy) เช่น 🦄, ✨, 🚀, 💪, 💎

## 2. การเข้าถึงและความเท่าเทียม (Accessibility - WCAG)

- **ARIA Labels:** ปุ่มที่มีแต่ไอคอนต้องมี `aria-label` เสมอ
- **Semantic HTML:** ใช้ `button` สำหรับงานที่โต้ตอบได้ แทนการใช้ `div` แล้วใส่ `onClick`
- **Keyboard Navigation:** ตรวจสอบให้แน่ใจว่าสามารถกด Tab เพื่อเลื่อนไปยังปุ่มต่างๆ ได้

## 3. อัตลักษณ์ของ AI Coach (Nong Uni)

- **System Instructions:** รักษาแนวคิด "ที่ปรึกษาทางการตลาด (UBC)" แทนการใช้คำว่านักขายตรง
- **Tone of Voice:** ต้องมีความสนุกสนาน, กระตือรือร้น, และเป็นกันเอง (Positive Energy Coach)
- **API Management:** ใช้ระบบบันทึก API Key ผ่าน `localStorage` เท่านั้น ไม่ต้องผ่าน Server เพื่อความปลอดภัย

## 4. ขั้นตอนการนำขึ้นระบบ (Deployment Workflow)

1. **Audit:** ตรวจสอบความเรียบร้อยของหน้าจอและ Accessibility
2. **Build:** รันคำสั่ง Build เพื่อเช็ค Error
3. **Commit:** ใช้ Commit message ที่สื่อความหมาย (เช่น `refactor: improve dashboard accessibility`)
4. **Push:** อัปเดตขึ้น GitHub เพื่อทริกเกอร์ระบบ Deploy อัตโนมัติ (GitHub Actions)

---
*จัดทำโดย Uni Smart AI Team*
